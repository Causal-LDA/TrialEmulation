# Trial Sequence class

Trial Sequence class

## Slots

- `data`:

  te_data.

- `estimand`:

  character. Descriptive name of estimand.

- `expansion`:

  te_expansion

- `outcome_model`:

  te_outcome_model.

- `outcome_data`:

  te_outcome_data.

- `censor_weight`:

  te_weight. Object to define weighting to account for informative
  censoring

- `censor_weight`:

  te_weight. Object to define weighting to account for informative
  censoring due to treatment switching
