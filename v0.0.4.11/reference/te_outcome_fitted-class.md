# Fitted Outcome Model Object

Fitted Outcome Model Object

## Slots

- `model`:

  list containing fitted model objects.

- `summary`:

  list of data.frames. Tidy model summaries a la `broom()` and
  `glance()`
